package com.example.mvvm

 open class BaseClass {
// Base Class - so that we can inherit its values
    //open is used so that i can be inherited
    fun coreValues(){
        println("core values of base class")
    }
}